[nixpacks
]
start_cmd = "reflex run --env prod --backend-only"[phases.setup
]
nixPkgs = [
    "python311",
    "nodejs",
    "unzip"
]
cmds = [
    "pip install -r scripts/requirements.txt"
]

[phases.build
]
cmds = [
    "reflex init"
]